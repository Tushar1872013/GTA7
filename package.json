{
  "name": "open-city-bike-3d",
  "version": "0.5.0",
  "description": "GTA-style browser game (Phase 1-5) with Vite + Three.js + Cannon-es + WebSocket multiplayer",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "server": "node server/multiplayer-server.js"
  },
  "dependencies": {
    "cannon-es": "^0.20.0",
    "three": "^0.160.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "vite": "^5.0.10"
  }
}
